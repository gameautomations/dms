{
  "name": "dmtest",
  "version": "0.0.0",
  "description": "dmtest",
  "main": "app.js",
  "author": {
    "name": ""
  },
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only main.ts",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@types/node": "^14.14.7",
    "prettier": "^2.5.1",
    "ts-node": "^10.4.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.0.5"
  }
}
